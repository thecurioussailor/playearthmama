// import { Router } from "express";

// export const groupRoutes = Router();

// groupRoutes.post('/', createGroup);
// groupRoutes.get('/', getGroup);
// groupRoutes.put('/:id', updateGroup);
// groupRoutes.delete('/:id', deleteGroup);
